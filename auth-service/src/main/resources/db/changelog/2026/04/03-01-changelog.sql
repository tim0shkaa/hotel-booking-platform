-- liquibase formatted sql

-- changeset tim0shkaa:1775213335019-1
CREATE TABLE users (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, email VARCHAR(255) NOT NULL, password_hash VARCHAR(255) NOT NULL, role VARCHAR(255) NOT NULL, active BOOLEAN NOT NULL, created_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, last_login_at TIMESTAMP WITHOUT TIME ZONE, CONSTRAINT pk_users PRIMARY KEY (id));

-- changeset tim0shkaa:1775213335019-2
CREATE TABLE refresh_tokens (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, user_id BIGINT NOT NULL, token_hash VARCHAR(255) NOT NULL, expires_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, revoked BOOLEAN NOT NULL, created_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, CONSTRAINT pk_refresh_tokens PRIMARY KEY (id));

-- changeset tim0shkaa:1775213335019-3
CREATE TABLE audit_logs (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, user_id BIGINT, action VARCHAR(255) NOT NULL, ip_address VARCHAR(255), user_agent VARCHAR(255), created_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, CONSTRAINT pk_audit_logs PRIMARY KEY (id));

-- changeset tim0shkaa:1775213335019-4
ALTER TABLE users ADD CONSTRAINT UC_USERS_EMAIL UNIQUE (email);

-- changeset tim0shkaa:1775213335019-5
ALTER TABLE refresh_tokens ADD CONSTRAINT UC_REFRESH_TOKENS_TOKEN_HASH UNIQUE (token_hash);

-- changeset tim0shkaa:1775213335019-6
ALTER TABLE refresh_tokens ADD CONSTRAINT FK_REFRESH_TOKENS_ON_USER FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE CASCADE;

-- changeset tim0shkaa:1775213335019-7
ALTER TABLE audit_logs ADD CONSTRAINT FK_AUDIT_LOGS_ON_USER FOREIGN KEY (user_id) REFERENCES users (id) ON DELETE SET NULL;